# OpenClaw-Py 待办清单 (2026-03-02, 更新)

> 基于 `docs/reference/` 全量参考文档与 `src/pyclaw/` 代码实际状态交叉核验。
>
> 已完成: Phase 0–58 (~57k LOC, 400+ .py, 1848+ 测试, 25 通道)
>
> Phase 54-58 重点修复: Browser RPC 真执行、Chat 高级能力接入、System/Logs RPC-first、Extended 去占位、文档基线重建

---

## 一、未实施的计划功能 (来自 nanobot_comparison_plan.md Phase 52–53)

### 1.1 Agent 社交网络 — Phase 52 ✅ 已实施

- [x] `src/pyclaw/social/registry.py` — 社交平台注册表 + `SocialPlatform` Protocol
- [x] `src/pyclaw/social/moltbook.py` — Moltbook 平台适配 (join/leave/send/poll)
- [x] `src/pyclaw/social/clawdchat.py` — ClawdChat 平台适配 (rooms/messaging)
- [x] 社交技能包 `skills/social/SKILL.md` — ✅ 已创建
- [x] Agent 工具: `social_join` / `social_status` — ✅ 已接入 tools 注册表 (`tools/social_tools.py` + `registry.py`)

### 1.2 Progress Streaming — Phase 53 ✅ 已实施

- [x] `src/pyclaw/agents/progress.py` — `ProgressEvent` + `ProgressTracker` 上下文管理器
- [x] Gateway 事件系统中添加 `EVENT_PROGRESS` 广播 (`gateway/events.py`)
- [x] 工具执行 (exec_tool) + 技能安装 (marketplace) 中已嵌入 `ProgressTracker` 调用
- [x] Flet UI 进度条展示 — ✅ `ChatView.update_progress()` + ProgressRing + 全局 progress listener

### 1.3 语音转录完善 — Phase 53 ✅ 已实施

- [x] Telegram 通道: 语音/音频消息自动下载 → Whisper API 转录 → 文本传入 Agent
- [x] 支持 Groq Whisper (免费优先) 和 OpenAI Whisper 两种后端
- [x] Flet 语音面板: 文件选择 (`FilePicker`) + 转录功能完善 — ✅ 已实现

---

## 一.5、差距更新 (Phase 54-58) ✅ 已完成

### Phase 54: Browser RPC 真执行化 ✅

- [x] `src/pyclaw/gateway/methods/browser_methods.py` — 重写为复用 Playwright 真执行器 (BrowserRuntime)
- [x] 全部 browser.* 方法改为 Playwright page 驱动 (screenshot 输出真实 PNG、snapshot 输出真实 HTML)
- [x] 补齐 `browser.profiles/createProfile/deleteProfile/focus/close` RPC 端实现
- [x] SSRF NavigationGuard 集成到 open/navigate

### Phase 55: Chat 主路径能力收敛 ✅

- [x] `src/pyclaw/gateway/methods/chat.py` — chat.send 接入 chat_advanced 的 validate_chat_params/sanitize_content/inject_time_context
- [x] 新增 `chat.edit` (编辑最后一条用户消息并重运行)
- [x] 新增 `chat.resend` (重新发送最后一条用户消息)
- [x] ChatAbortManager 统一管理 abort 状态
- [x] embedded/legacy 两条路径共享 _do_chat_run，usage/abort 一致

### Phase 56: System/Logs RPC-first ✅

- [x] `src/pyclaw/gateway/methods/extended.py` — 新增 system.event/system.heartbeat.last/system.presence RPC
- [x] `src/pyclaw/cli/commands/system_cmd.py` — fallback 时输出显式 `[warn] Gateway unreachable` 提示
- [x] `src/pyclaw/cli/commands/logs_cmd.py` — fallback 时输出显式警告

### Phase 57: Extended 去占位 ✅

- [x] `wizard.start/wizard.step` → NOT_IMPLEMENTED (提示使用 `pyclaw setup`)
- [x] `push.send` → NOT_IMPLEMENTED (提示配置 FCM/APNs)
- [x] `voicewake.status` → NOT_IMPLEMENTED
- [x] `tts.speak` → NOT_IMPLEMENTED (提示配置 TTS 提供商)
- [x] `update.check` → 读取真实版本号 + note 说明
- [x] `web.status` → 反映 Gateway WebSocket 真实状态 + uptime

### Phase 58: 文档基线重建 ✅

- [x] `docs/api-reference.md` — 更新 chat/browser/system/logs/extended 方法文档
- [x] `docs/reference/PROGRESS.md` — 追加 Phase 52-58 条目
- [x] 本文件更新

---

## 二、代码中的占位 / 未生产化实现

### 2.1 Provider Usage API — ✅ 已去占位

- [x] `src/pyclaw/infra/provider_usage.py` — OpenAI org usage API + Anthropic/Google key 验证（真实 HTTP 调用）

### 2.2 CLI screenshot — ✅ 已去占位

- [x] `src/pyclaw/cli/app.py` — docstring 已修正，实际实现经由 Gateway RPC 调用 browser.screenshot

### 2.3 security audit --fix — ✅ 已实现

- [x] `src/pyclaw/cli/commands/security_cmd.py` — 自动修复: chmod 600 配置文件 + 重绑 gateway 到 127.0.0.1

### 2.4 plugins/contrib/misc_extensions.py — 框架/存根

- [x] Lobster / LLM-Task / Copilot Proxy / OTEL 扩展 — ✅ 已去存根: Lobster 支持 async step 执行 + 审批门, LLM-Task 支持直接调用 API, Copilot Proxy 支持 chat completion, OTEL 支持 HTTP 导出到 OTLP collector

### 2.5 Onboarding 通道向导 — 未全部完善

- [x] `src/pyclaw/channels/plugins/onboarding.py` — ✅ 已扩展: 新增 Matrix/Feishu/MSTeams/DingTalk/QQ/IRC 六个通道向导 (共 12 通道)

### 2.6 未使用的声明依赖 — ✅ 已清理

- [x] `loguru` — 已从 `pyproject.toml` 移除 (决策: 继续使用 stdlib logging)
- [x] `aiosqlite` — 已从 `pyproject.toml` 移除 (LanceDB 覆盖向量需求，sync sqlite3 足够)
- [x] `watchdog` — 已从 `pyproject.toml` 移除 (config_reload 使用轮询已足够)

### 2.7 sqlite-vec 缺失 — 计划功能未落地

- [ ] 原始重写方案计划使用 `aiosqlite + sqlite-vec` 进行向量搜索，实际仅有 LanceDB 后端
- [ ] `memory/store.py` 使用同步 `sqlite3` + FTS5 (纯关键词搜索)，无向量能力
- 说明: LanceDB 已覆盖向量搜索需求，sqlite-vec 可视为被 LanceDB 替代；但如需轻量级无额外依赖的向量搜索，仍可补充

### 2.8 插件系统 entry_points 接入启动流程 — ✅ 已实现

- [x] `gateway/server.py` 的 `create_gateway_app()` 现在调用 `_load_plugins()` 自动发现并注册第三方插件

### 2.9 Config 热重载接入 Gateway — ✅ 已实现

- [x] `gateway/server.py` 的 `create_gateway_app()` 现在调用 `_start_config_watcher()` 启动后台轮询
- [x] 通过 FastAPI startup/shutdown 事件管理 watcher 生命周期

---

## 三、原始重写方案中规划但未完整实现的功能 (来自 python-flet-rewrite-plan.md)

### 3.0 Flet UI 功能缺口

> 计划中的 UI 组件未全部落地:

- [x] Agents 管理页面 — ✅ `ui/agents_panel.py` Agent 列表/创建/详情 + NavigationRail 导航集成
- [x] 独立 toolbar 组件 — ✅ `ui/toolbar.py` 模型选择 + 附件 + 语音 + 清空
- [x] theme.py 主题模块 — ✅ `ui/theme.py` Light/Dark 双色方案 + Flet ThemeData 转换 + 全局切换
- [x] 移动端适配模块 — ✅ `ui/permissions.py` 移动端权限检查/请求 + 桌面端自动授权
- [x] 桌面 menubar 模块 — ✅ `ui/menubar.py` File/View/Help 三级菜单
- [x] `media/fetch.py` 媒体下载模块 — ✅ 远程媒体下载 + 缓存 + MIME 验证 + 大小限制
- 说明: 多数是计划中的模块拆分，实际功能内联在其他文件中，**按需重构**

### 3.1 Voice Wake 语音唤醒 — 仅有配置骨架

- [x] `VoiceWakeState.start_listening_async()` — ✅ sounddevice + vosk 集成实现
- [x] 后台 asyncio task 持续监听麦克风，vosk 识别唤醒词
- [x] 可配置 wake word / sensitivity，wake 回调通知上层
- 需要用户安装 `sounddevice` + `vosk` + Vosk 模型文件

---

## 四、TS 项目存在但 Python 未覆盖的功能 (来自 20260301gap.md)

### 4.1 Browser 自动化深度 — 覆盖率 ~35% (升级自 ~1%)

> Phase 54 后: Gateway browser RPC 已改为 Playwright 真执行，补齐 16 个 RPC 方法。

- [x] Browser RPC 真执行 (start/stop/open/navigate/click/type/screenshot/snapshot/evaluate)
- [x] Profile 管理 RPC (profiles/createProfile/deleteProfile)
- [x] Tab 管理 RPC (tabs/focus/close)
- [x] SSRF NavigationGuard 集成
- [ ] CDP (Chrome DevTools Protocol) 直接通信
- [ ] Chrome/Brave/Edge 可执行文件发现
- [ ] Playwright AI 模块集成
- [ ] Client Actions 高级操作 (核心/观察/状态/URL)
- [ ] Server 生命周期管理 (中间件/热重载)
- 说明: Python 使用 Playwright 作为统一入口，核心 RPC 链路已打通

### 4.2 Channel Plugins 深度 — 覆盖率 ~15%

> Python 有 Plugin SDK + 6 通道 catalog/onboarding/outbound，TS 有 48+ plugin 文件 + 602 extension 文件。

- [x] 剩余通道 Per-channel Outbound 适配 — ✅ 新增 Matrix/Feishu/MSTeams/DingTalk/IRC/Mattermost/GoogleChat/LINE/QQ/Twitch/Nostr (共 17 通道)
- [x] 剩余通道 Status Issues 检查器 — ✅ 新增 Slack/Matrix/Feishu/MSTeams/DingTalk/IRC (共 11 通道) + 对应 Config Schema
- [x] Per-channel Actions 规范 — ✅ `plugins/actions.py` 17 通道动作能力定义 + `ChannelActionsHelper`
- [x] Per-channel Normalize 标准化 — ✅ `plugins/normalize.py` 17 通道消息标准化规则 + `normalize_inbound()` 函数
- [x] WhatsApp Heartbeat 连接检查 — ✅ `whatsapp/channel.py` 后台心跳循环 + 自动重连
- [x] Account Helpers (账号列表/ID) — ✅ `plugins/account_helpers.py` 16 通道账号能力定义 + `AccountHelper`
- [ ] Extension Runtimes (40 个扩展独立 runtime)

### 4.3 TUI (Terminal UI) — 覆盖率 0%

> Python 使用 Flet GUI，但 TS TUI 的某些功能在 Python 中无等价:

- [ ] Terminal 聊天界面 (gateway-chat)
- [ ] 输入历史
- [ ] 本地 Shell 集成
- [ ] 会话切换/列表
- [ ] Overlay (帮助/状态)
- [ ] 15 个终端组件
- 说明: 这是架构设计差异 (Flet GUI vs TUI)，**可选实现**

### 4.4 Azure OpenAI 提供商适配 — ✅ 核心已实现

- [x] `src/pyclaw/agents/providers/azure_openai.py` — Azure OpenAI Service 适配 (deployment 路由 + API key / AAD 认证 + SSE 流式)
- [x] 配置 schema 中添加 Azure OpenAI 字段 (`deployment`, `apiVersion`, `useAad`, `aadTenantId`, `aadClientId`)
- [x] CLI `auth login --provider azure-openai` — ✅ 已在 auth_cmd.py 中注册为 API key 认证方式

### 4.5 其它小差距

- [x] Discord Voice 通道 — ✅ `discord/voice.py` join/leave/speak_tts/record_and_transcribe
- [ ] Push APNS (iOS 推送通知)
- [x] Clipboard 系统剪贴板访问 — ✅ `tools/clipboard_tool.py` read/write 工具
- [x] Process Respawn 自动重启 — ✅ `ProcessRespawner` 完整实现 asyncio subprocess 管理 + 指数退避重启
- [ ] Per-channel Zod→JSON Config 类型 (~10 文件) — ✅ Config schemas 已在 status_issues.py 中覆盖
- [x] Legacy 配置迁移 v2/v3 — ✅ 已有完整实现 (`config/migrations.py` v1→v2→v3)
- [ ] Agents Schema (Gemini Schema 清理 + TypeBox)
- [x] CLI Runner (watchdog + resume) — ✅ `--resume` 参数支持恢复最近会话
- [ ] Plugin Runtime (native deps, hooks 完整)

---

## 五、持续追踪项

- [ ] 持续跟踪 TS 主项目新 commit，识别新差距

> 以下经评估 **不列入 TODO**（不在 Python 项目射程内或属于生态/日常维护）:
> - 原生 macOS/iOS/Android 深度对齐 — 需 Swift/Kotlin，属独立 companion app
> - CI/CD 与 TS 完全对齐 — 已有基础 CI/CD，增量优化属日常维护
> - 完整 Extension marketplace — 需 npm 生态建设，非代码任务
> - TS 2026.2.25 Android Chat UI — Flet 已覆盖移动端
> - Extension 完整 Runtime — 每扩展 40+ 文件，LOC 巨大，按需推进
> - macOS Device Model 数据库 — 属于 Swift companion app 资产
> - MiniMax / Google model ID 修复 — 已有基础，按需跟进

---

## 六、工程质量与基础设施

### 6.1 测试

- [x] 端到端集成测试 — ✅ `test_e2e_gateway.py` Gateway/Agent/Config/Progress/Media 全链路
- [x] 性能基准测试 — ✅ `test_benchmark.py` agent loop latency + token throughput + session scaling + abort + progress (7 tests, all pass)
- [x] MCP 集成测试 — ✅ `test_mcp_integration.py` stdio transport + HTTP transport + client lifecycle + registry + tool adapter (12 tests, all pass)
- [x] Docker 构建 + 冒烟测试 — ✅ `test_docker_smoke.py` 18 模块导入 + CLI + Schema + Registry (26 tests, all pass)

### 6.2 文档

- [x] 用户文档: 安装 / 快速开始 / 配置参考 / 通道接入指南 — ✅ README.md 已覆盖完整用户文档
- [x] API 参考文档 (Gateway WebSocket / OpenAI compat / MCP) — ✅ `docs/api-reference.md`
- [x] 贡献指南 (CONTRIBUTING.md) — ✅ `CONTRIBUTING.md`
- [x] 架构设计文档 (面向开发者) — ✅ `docs/architecture.md`

### 6.3 发布 / 分发

- [x] PyPI 首次发布 (`pyclaw` 包名) — ✅ pyproject.toml 补全 authors/classifiers/urls/keywords, release.yml 已配置 tag-based publish
- [x] Docker Hub 镜像发布 — ✅ release.yml 扩展: Docker Hub + GHCR 双发布, linux/amd64+arm64 multi-arch
- [x] Homebrew formula (macOS) — ✅ `Formula/pyclaw.rb` + README 安装说明更新 (pipx/brew/docker)
- [x] Release notes 自动生成 — ✅ `scripts/generate-release-notes.sh` 基于 git log + Conventional Commits 分类

---

## 七、优先级总结

| 优先级 | 类别 | 已完成 / 总数 | 预估剩余 |
|--------|------|-------------|---------|
| **P0** | 占位去生产化 + 无用依赖清理 + 插件/热重载接入启动流程 | **9/9** ✅ | 0 天 |
| **P1** | Agent 社交网络 (Phase 52) | **5/5** ✅ | 0 天 |
| **P2** | Progress Streaming + 语音转录 (Phase 53) | **8/8** ✅ | 0 天 |
| **P2** | Browser 深度 / Channel Plugin 深度 / Azure OpenAI | **9/13** | ~1.5 天 |
| **P2** | Flet UI 缺口 + Voice Wake + Onboarding + misc_extensions | **11/11** ✅ | 0 天 |
| **P3** | TUI / Discord Voice / Push / 小差距 | **8/15** | ~1.5 天 |
| **P3** | 工程质量 (测试/文档/发布/CI 契约守护) | **12/13** | ~0.5 天 |

**Phase 54-67 差距更新已完成。Phase 总进度: 76/79 条完成，剩余 ~1.5 人天**

### CoPaw 集成 (Phase 62-67) ✅ 已完成

- [x] Phase 62: 本地模型运行时 (llama.cpp + MLX + HuggingFace/ModelScope 下载 + manifest 管理)
- [x] Phase 63: 桌面截图 (mss 跨平台) + 文件发送 (send_file) 工具
- [x] Phase 64: 一键安装脚本 (install.sh/install.ps1 + uninstall 命令)
- [x] Phase 65: Office 文件技能包 (PDF/DOCX/XLSX/PPTX 读取工具 + SKILL.md)
- [x] Phase 66: Heartbeat 增强 (HEARTBEAT.md + compound 间隔 + target=last 通道分发)
- [x] Phase 67: MCP 热重载 (McpConfigWatcher 配置变更自动重连)

---

## 八、与 upstream TypeScript 项目的持续对比流程

> Phase 61 新增。防止文档与实现再次漂移。

### 8.1 自动守护

- CI 中独立 step `Contract tests (docs-RPC parity)` 每次 push/PR 必跑，包含：
  - `test_rpc_contract_phase58.py` — 文档声明的 RPC 方法 ↔ 注册方法一致性
  - `test_docs_parity_phase43.py` — 文档-命令契约
  - `test_naming_parity_phase49.py` — 命名一致性
- 新增 RPC 方法或 CLI 命令时，必须同步更新 `docs/api-reference.md` 和 PROGRESS.md，否则 CI 失败。

### 8.2 季度手动复核流程

每季度（1/4/7/10 月第一周）执行一次：

1. **查看 TS upstream CHANGELOG / Releases**  
   `https://github.com/openclaw/openclaw/releases` — 检查上一季度新增的 RPC 方法、CLI 命令、通道、提供商。
2. **对比 RPC 方法清单**  
   将 upstream 的 `docs/api-reference.md`（或等价）与本项目 `docs/api-reference.md` 做 diff，记录新增/变更项。
3. **评估优先级**  
   按 P0-P3 分类新增差距项，更新本文档 `七、优先级总结` 表。
4. **创建执行计划**  
   对 P0/P1 项立即排期，P2/P3 项按资源情况排入后续 Phase。
5. **更新 PROGRESS.md**  
   在功能对齐状态一节追加季度复核记录。

---

## 参考文档

- [PROGRESS.md](PROGRESS.md) — 执行进度
- [20260301gap.md](20260301gap.md) — 深度差距分析
- [nanobot_comparison_plan.md](nanobot_comparison_plan.md) — nanobot 对比与 Phase 50-53 计划
- [20260301_plan.md](20260301_plan.md) — Phase 18-49 实施计划
- [copaw_comparison_plan.md](copaw_comparison_plan.md) — CoPaw 对比与 Phase 62-67 集成计划
- [python-flet-rewrite-plan.md](python-flet-rewrite-plan.md) — 原始重写方案
